[project]
name = "docling-scripts"
version = "0.1.0"
description = "Document conversion scripts using Docling"
requires-python = ">=3.12,<3.14"
dependencies = [
    "docling>=2.95.0",
]

[dependency-groups]
dev = []
